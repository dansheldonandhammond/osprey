{% doc %}
  @prompt
    Create a Shopify blog template section with AJAX tag filtering featuring a sticky black header with centered "Blog" title and horizontal navigation menu for category filtering (Home, Latest News, Guides & How To, Athletes & Ambassadors, Community). Display blog posts in a responsive 3-column grid collapsing to single column under 768px. Each card includes featured image with hover scale effect, category tag link, uppercase title, excerpt, and reading time at bottom. Implement AJAX functionality using vanilla JavaScript so navigation tabs dynamically filter posts by tag without page refresh, with smooth transitions and scroll context. Include loading spinner, error handling, and load more pagination. Grid uses consistent card layouts with 3:2 aspect ratio images, generous spacing, and hover interactions (translateY lift, box-shadow). Style with modern minimalist design: black header, white cards on light gray background, clean typography. Navigation shows active state with white underline at full opacity, inactive at 70%. Mobile navigation becomes horizontally scrollable pills with rounded backgrounds. Cards include schema.org BlogPosting markup, responsive image srcsets, semantic HTML5 elements, ARIA labels, focus states, and reduced motion support. Calculate reading time from word count (200 words/minute). Create reusable blog-card snippet accepting article object. Use Shopify Liquid with schema JSON for settings: blog handle, posts per page (6-24), page title, navigation blocks with tag labels and handles. AJAX updates grid innerHTML on filter, appends on load more, hides pagination when exhausted. Ensure SEO with alt text, meta tags, proper hierarchy, fast loads. Matches industrial-modern aesthetic with professional presentation.
{% enddoc %}
{% assign ai_gen_id = section.id | replace: '_', '' | downcase %}

{% style %}
  .ai-blog-section-{{ ai_gen_id }} {
    width: 100%;
    background-color: #f5f5f5;
    min-height: 100vh;
  }

  .ai-blog-header-{{ ai_gen_id }} {
    background-color: #000000;
    color: #ffffff;
    padding: 40px 20px;
    position: sticky;
    top: 0;
    z-index: 100;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  }

  .ai-blog-header-content-{{ ai_gen_id }} {
    max-width: 1300px;
    margin: 0 auto;
  }

  .ai-blog-title-{{ ai_gen_id }} {
    text-align: center;
    font-size: 32px;
    font-weight: 600;
    margin: 0 0 24px;
    letter-spacing: 0.5px;
  }

  .ai-blog-nav-{{ ai_gen_id }} {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 40px;
    flex-wrap: wrap;
  }

  .ai-blog-nav-button-{{ ai_gen_id }} {
    background: none;
    border: none;
    color: #ffffff;
    font-size: 15px;
    font-weight: 500;
    cursor: pointer;
    padding: 8px 0;
    position: relative;
    opacity: 0.7;
    transition: opacity 0.3s ease;
    white-space: nowrap;
  }

  .ai-blog-nav-button-{{ ai_gen_id }}.active {
    opacity: 1;
  }

  .ai-blog-nav-button-{{ ai_gen_id }}.active::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 2px;
    background-color: #ffffff;
  }

  .ai-blog-container-{{ ai_gen_id }} {
    max-width: 1300px;
    margin: 0 auto;
    padding: 60px 20px;
  }

  .ai-blog-grid-{{ ai_gen_id }} {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
    margin-bottom: 60px;
    opacity: 1;
    transition: opacity 0.3s ease;
  }

  .ai-blog-grid-{{ ai_gen_id }}.loading {
    opacity: 0.5;
    pointer-events: none;
  }

  .ai-blog-card-{{ ai_gen_id }} {
    background-color: #ffffff;
    border-radius: 0;
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  }

  .ai-blog-card-{{ ai_gen_id }}:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
  }

  .ai-blog-card-image-wrapper-{{ ai_gen_id }} {
    position: relative;
    width: 100%;
    padding-bottom: 66.67%;
    overflow: hidden;
    background-color: #f0f0f0;
  }

  .ai-blog-card-image-{{ ai_gen_id }} {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }

  .ai-blog-card-{{ ai_gen_id }}:hover .ai-blog-card-image-{{ ai_gen_id }} {
    transform: scale(1.05);
  }

  .ai-blog-card-content-{{ ai_gen_id }} {
    padding: 24px;
  }

  .ai-blog-card-tag-{{ ai_gen_id }} {
    display: inline-block;
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: #000000;
    text-decoration: none;
    margin-bottom: 12px;
    transition: opacity 0.3s ease;
  }

  .ai-blog-card-tag-{{ ai_gen_id }}:hover,
  .ai-blog-card-tag-{{ ai_gen_id }}:focus {
    opacity: 0.7;
    outline: 2px solid rgba(0, 0, 0, 0.3);
    outline-offset: 2px;
  }

  .ai-blog-card-title-{{ ai_gen_id }} {
    font-size: 18px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin: 0 0 12px;
    line-height: 1.4;
  }

  .ai-blog-card-title-link-{{ ai_gen_id }} {
    color: #000000;
    text-decoration: none;
    transition: opacity 0.3s ease;
  }

  .ai-blog-card-title-link-{{ ai_gen_id }}:hover,
  .ai-blog-card-title-link-{{ ai_gen_id }}:focus {
    opacity: 0.7;
    outline: 2px solid rgba(0, 0, 0, 0.3);
    outline-offset: 2px;
  }

  .ai-blog-card-excerpt-{{ ai_gen_id }} {
    font-size: 14px;
    line-height: 1.6;
    color: #666666;
    margin: 0 0 16px;
  }

  .ai-blog-card-meta-{{ ai_gen_id }} {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 12px;
    color: #999999;
    padding-top: 16px;
    border-top: 1px solid #e5e5e5;
  }

  .ai-blog-card-reading-time-{{ ai_gen_id }} {
    font-weight: 500;
  }

  .ai-blog-loader-{{ ai_gen_id }} {
    display: none;
    text-align: center;
    padding: 40px;
  }

  .ai-blog-loader-{{ ai_gen_id }}.active {
    display: block;
  }

  .ai-blog-spinner-{{ ai_gen_id }} {
    display: inline-block;
    width: 40px;
    height: 40px;
    border: 4px solid #f0f0f0;
    border-top-color: #000000;
    border-radius: 50%;
    animation: ai-blog-spin-{{ ai_gen_id }} 0.8s linear infinite;
  }

  @keyframes ai-blog-spin-{{ ai_gen_id }} {
    to {
      transform: rotate(360deg);
    }
  }

  .ai-blog-load-more-{{ ai_gen_id }} {
    text-align: center;
    margin-top: 40px;
  }

  .ai-blog-load-more-button-{{ ai_gen_id }} {
    background-color: #000000;
    color: #ffffff;
    border: none;
    padding: 16px 48px;
    font-size: 14px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }

  .ai-blog-load-more-button-{{ ai_gen_id }}:hover {
    background-color: #333333;
  }

  .ai-blog-load-more-button-{{ ai_gen_id }}:focus {
    outline: 2px solid #000000;
    outline-offset: 4px;
  }

  .ai-blog-load-more-button-{{ ai_gen_id }}:disabled {
    background-color: #cccccc;
    cursor: not-allowed;
  }

  .ai-blog-error-{{ ai_gen_id }} {
    display: none;
    text-align: center;
    padding: 40px;
    color: #d32f2f;
    font-size: 14px;
  }

  .ai-blog-error-{{ ai_gen_id }}.active {
    display: block;
  }

  .ai-blog-empty-{{ ai_gen_id }} {
    text-align: center;
    padding: 80px 20px;
    color: #666666;
  }

  .ai-blog-empty-{{ ai_gen_id }} h3 {
    font-size: 24px;
    margin: 0 0 12px;
  }

  @media (prefers-reduced-motion: reduce) {
    .ai-blog-card-{{ ai_gen_id }},
    .ai-blog-card-image-{{ ai_gen_id }},
    .ai-blog-nav-button-{{ ai_gen_id }},
    .ai-blog-card-tag-{{ ai_gen_id }},
    .ai-blog-card-title-link-{{ ai_gen_id }},
    .ai-blog-load-more-button-{{ ai_gen_id }} {
      transition: none;
    }

    .ai-blog-spinner-{{ ai_gen_id }} {
      animation: none;
    }
  }

  @media screen and (max-width: 989px) {
    .ai-blog-grid-{{ ai_gen_id }} {
      grid-template-columns: repeat(2, 1fr);
      gap: 24px;
    }
  }

  @media screen and (max-width: 767px) {
    .ai-blog-header-{{ ai_gen_id }} {
      padding: 24px 16px;
    }

    .ai-blog-title-{{ ai_gen_id }} {
      font-size: 24px;
      margin-bottom: 20px;
    }

    .ai-blog-nav-{{ ai_gen_id }} {
      gap: 0;
      justify-content: flex-start;
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none;
      -ms-overflow-style: none;
      padding-bottom: 4px;
    }

    .ai-blog-nav-{{ ai_gen_id }}::-webkit-scrollbar {
      display: none;
    }

    .ai-blog-nav-button-{{ ai_gen_id }} {
      padding: 8px 16px;
      border-radius: 20px;
      margin-right: 8px;
      background-color: rgba(255, 255, 255, 0.1);
      flex-shrink: 0;
    }

    .ai-blog-nav-button-{{ ai_gen_id }}.active {
      background-color: rgba(255, 255, 255, 0.2);
    }

    .ai-blog-nav-button-{{ ai_gen_id }}.active::after {
      display: none;
    }

    .ai-blog-container-{{ ai_gen_id }} {
      padding: 40px 16px;
    }

    .ai-blog-grid-{{ ai_gen_id }} {
      grid-template-columns: 1fr;
      gap: 24px;
      margin-bottom: 40px;
    }

    .ai-blog-card-content-{{ ai_gen_id }} {
      padding: 20px;
    }

    .ai-blog-card-title-{{ ai_gen_id }} {
      font-size: 16px;
    }
  }
{% endstyle %}

<blog-filter-{{ ai_gen_id }}
  class="ai-blog-section-{{ ai_gen_id }}"
  data-blog-handle="{{ section.settings.blog }}"
  data-posts-per-page="{{ section.settings.posts_per_page }}"
>
  <header class="ai-blog-header-{{ ai_gen_id }}">
    <div class="ai-blog-header-content-{{ ai_gen_id }}">
      <h1 class="ai-blog-title-{{ ai_gen_id }}">{{ section.settings.page_title }}</h1>
      <nav class="ai-blog-nav-{{ ai_gen_id }}" role="navigation" aria-label="Blog categories">
        <button
          class="ai-blog-nav-button-{{ ai_gen_id }} active"
          data-tag=""
          aria-label="Show all posts"
          aria-pressed="true"
        >
          {{ section.settings.nav_all_label }}
        </button>
        {% if section.settings.nav_1_label != blank %}
          <button
            class="ai-blog-nav-button-{{ ai_gen_id }}"
            data-tag="{{ section.settings.nav_1_tag }}"
            aria-label="Filter by {{ section.settings.nav_1_label }}"
            aria-pressed="false"
          >
            {{ section.settings.nav_1_label }}
          </button>
        {% endif %}
        {% if section.settings.nav_2_label != blank %}
          <button
            class="ai-blog-nav-button-{{ ai_gen_id }}"
            data-tag="{{ section.settings.nav_2_tag }}"
            aria-label="Filter by {{ section.settings.nav_2_label }}"
            aria-pressed="false"
          >
            {{ section.settings.nav_2_label }}
          </button>
        {% endif %}
        {% if section.settings.nav_3_label != blank %}
          <button
            class="ai-blog-nav-button-{{ ai_gen_id }}"
            data-tag="{{ section.settings.nav_3_tag }}"
            aria-label="Filter by {{ section.settings.nav_3_label }}"
            aria-pressed="false"
          >
            {{ section.settings.nav_3_label }}
          </button>
        {% endif %}
        {% if section.settings.nav_4_label != blank %}
          <button
            class="ai-blog-nav-button-{{ ai_gen_id }}"
            data-tag="{{ section.settings.nav_4_tag }}"
            aria-label="Filter by {{ section.settings.nav_4_label }}"
            aria-pressed="false"
          >
            {{ section.settings.nav_4_label }}
          </button>
        {% endif %}
        {% if section.settings.nav_5_label != blank %}
          <button
            class="ai-blog-nav-button-{{ ai_gen_id }}"
            data-tag="{{ section.settings.nav_5_tag }}"
            aria-label="Filter by {{ section.settings.nav_5_label }}"
            aria-pressed="false"
          >
            {{ section.settings.nav_5_label }}
          </button>
        {% endif %}
      </nav>
    </div>
  </header>

  <div class="ai-blog-container-{{ ai_gen_id }}">
    <div class="ai-blog-grid-{{ ai_gen_id }}" role="list"></div>

    <div class="ai-blog-loader-{{ ai_gen_id }}" role="status" aria-live="polite">
      <div class="ai-blog-spinner-{{ ai_gen_id }}"></div>
      <p style="margin-top: 16px;">Loading posts...</p>
    </div>

    <div class="ai-blog-error-{{ ai_gen_id }}" role="alert" aria-live="assertive">
      <p>Unable to load blog posts. Please try again later.</p>
    </div>

    <div class="ai-blog-load-more-{{ ai_gen_id }}" style="display: none;">
      <button class="ai-blog-load-more-button-{{ ai_gen_id }}" aria-label="Load more posts">
        Load More
      </button>
    </div>
  </div>
</blog-filter-{{ ai_gen_id }}>

<script>
  (function() {
    class BlogFilter{{ ai_gen_id }} extends HTMLElement {
      constructor() {
        super();
        this.blogHandle = this.dataset.blogHandle;
        this.postsPerPage = parseInt(this.dataset.postsPerPage) || 12;
        this.currentPage = 1;
        this.currentTag = '';
        this.isLoading = false;
        this.hasMorePosts = true;
        this.scrollPosition = 0;
      }

      connectedCallback() {
        this.grid = this.querySelector('.ai-blog-grid-{{ ai_gen_id }}');
        this.loader = this.querySelector('.ai-blog-loader-{{ ai_gen_id }}');
        this.errorMessage = this.querySelector('.ai-blog-error-{{ ai_gen_id }}');
        this.loadMoreContainer = this.querySelector('.ai-blog-load-more-{{ ai_gen_id }}');
        this.loadMoreButton = this.querySelector('.ai-blog-load-more-button-{{ ai_gen_id }}');
        this.navButtons = this.querySelectorAll('.ai-blog-nav-button-{{ ai_gen_id }}');

        this.setupEventListeners();
        this.loadPosts();
      }

      setupEventListeners() {
        this.navButtons.forEach(button => {
          button.addEventListener('click', () => {
            this.scrollPosition = window.pageYOffset;
            this.handleTagFilter(button);
          });
        });

        if (this.loadMoreButton) {
          this.loadMoreButton.addEventListener('click', () => {
            this.loadMorePosts();
          });
        }
      }

      handleTagFilter(button) {
        if (this.isLoading) return;

        this.navButtons.forEach(btn => {
          btn.classList.remove('active');
          btn.setAttribute('aria-pressed', 'false');
        });
        button.classList.add('active');
        button.setAttribute('aria-pressed', 'true');

        this.currentTag = button.dataset.tag;
        this.currentPage = 1;
        this.hasMorePosts = true;
        this.grid.innerHTML = '';

        this.loadPosts();
      }

      async loadPosts() {
        if (this.isLoading) return;

        this.isLoading = true;
        this.showLoader();
        this.hideError();

        try {
          const url = this.buildUrl();
          const response = await fetch(url);

          if (!response.ok) {
            throw new Error('Network response was not ok');
          }

          const html = await response.text();
          const parser = new DOMParser();
          const doc = parser.parseFromString(html, 'text/html');
          const articles = doc.querySelectorAll('.blog-articles article, article[itemtype*="BlogPosting"]');

          if (articles.length === 0) {
            this.hasMorePosts = false;
            if (this.currentPage === 1) {
              this.showEmptyState();
            }
          } else {
            this.renderArticles(articles);
            if (articles.length < this.postsPerPage) {
              this.hasMorePosts = false;
            }
          }

          this.updateLoadMoreButton();
          this.restoreScrollPosition();
        } catch (error) {
          this.showError();
        } finally {
          this.isLoading = false;
          this.hideLoader();
        }
      }

      async loadMorePosts() {
        if (this.isLoading || !this.hasMorePosts) return;

        this.currentPage++;
        this.isLoading = true;
        this.loadMoreButton.disabled = true;

        try {
          const url = this.buildUrl();
          const response = await fetch(url);

          if (!response.ok) {
            throw new Error('Network response was not ok');
          }

          const html = await response.text();
          const parser = new DOMParser();
          const doc = parser.parseFromString(html, 'text/html');
          const articles = doc.querySelectorAll('.blog-articles article, article[itemtype*="BlogPosting"]');

          if (articles.length === 0) {
            this.hasMorePosts = false;
          } else {
            this.appendArticles(articles);
            if (articles.length < this.postsPerPage) {
              this.hasMorePosts = false;
            }
          }

          this.updateLoadMoreButton();
        } catch (error) {
          this.currentPage--;
        } finally {
          this.isLoading = false;
          this.loadMoreButton.disabled = false;
        }
      }

      buildUrl() {
        let url = `/blogs/${this.blogHandle}`;
        
        if (this.currentTag) {
          url += `/tagged/${this.currentTag}`;
        }

        url += `?page=${this.currentPage}`;
        
        return url;
      }

      renderArticles(articles) {
        this.grid.innerHTML = '';
        this.appendArticles(articles);
      }

      appendArticles(articles) {
        articles.forEach(article => {
          const card = this.createCard(article);
          this.grid.appendChild(card);
        });
      }

      createCard(article) {
        const link = article.querySelector('a');
        const image = article.querySelector('img');
        const title = article.querySelector('.blog-card__title, .article-card__title, h3, h2');
        const excerpt = article.querySelector('.blog-card__excerpt, .article-card__excerpt, p');
        const tags = article.querySelectorAll('.blog-card__tag, .article-card__badge, .badge');
        const content = article.querySelector('.blog-card__content, .article-card__info');

        const card = document.createElement('article');
        card.className = 'ai-blog-card-{{ ai_gen_id }}';
        card.setAttribute('itemscope', '');
        card.setAttribute('itemtype', 'https://schema.org/BlogPosting');
        card.setAttribute('role', 'listitem');

        let imageHtml = '';
        if (image) {
          const imgSrc = image.src;
          const imgAlt = image.alt || (title ? title.textContent : 'Blog post image');
          const imgSrcset = image.srcset || '';
          
          imageHtml = `
            <div class="ai-blog-card-image-wrapper-{{ ai_gen_id }}">
              <img
                src="${imgSrc}"
                ${imgSrcset ? `srcset="${imgSrcset}"` : ''}
                alt="${imgAlt}"
                class="ai-blog-card-image-{{ ai_gen_id }}"
                loading="lazy"
                itemprop="image"
              >
            </div>
          `;
        } else {
          imageHtml = `
            <div class="ai-blog-card-image-wrapper-{{ ai_gen_id }}">
              <div class="ai-blog-card-image-{{ ai_gen_id }}" style="background-color: #f0f0f0;"></div>
            </div>
          `;
        }

        let tagHtml = '';
        if (tags.length > 0) {
          const firstTag = tags[0];
          const tagText = firstTag.textContent.trim();
          const tagLink = firstTag.href || '#';
          tagHtml = `<a href="${tagLink}" class="ai-blog-card-tag-{{ ai_gen_id }}" itemprop="keywords">${tagText}</a>`;
        }

        const titleText = title ? title.textContent.trim() : 'Untitled Post';
        const excerptText = excerpt ? excerpt.textContent.trim() : '';
        const articleUrl = link ? link.href : '#';
        
        const wordCount = this.countWords(content ? content.textContent : excerptText);
        const readingTime = Math.max(1, Math.ceil(wordCount / 200));

        card.innerHTML = `
          ${imageHtml}
          <div class="ai-blog-card-content-{{ ai_gen_id }}">
            ${tagHtml}
            <h2 class="ai-blog-card-title-{{ ai_gen_id }}">
              <a href="${articleUrl}" class="ai-blog-card-title-link-{{ ai_gen_id }}" itemprop="headline">
                ${titleText}
              </a>
            </h2>
            ${excerptText ? `<p class="ai-blog-card-excerpt-{{ ai_gen_id }}" itemprop="description">${excerptText}</p>` : ''}
            <div class="ai-blog-card-meta-{{ ai_gen_id }}">
              <span class="ai-blog-card-reading-time-{{ ai_gen_id }}">${readingTime} min read</span>
            </div>
          </div>
          <meta itemprop="url" content="${articleUrl}">
        `;

        return card;
      }

      countWords(text) {
        if (!text) return 0;
        return text.trim().split(/\s+/).length;
      }

      showLoader() {
        this.loader.classList.add('active');
        this.grid.classList.add('loading');
      }

      hideLoader() {
        this.loader.classList.remove('active');
        this.grid.classList.remove('loading');
      }

      showError() {
        this.errorMessage.classList.add('active');
      }

      hideError() {
        this.errorMessage.classList.remove('active');
      }

      showEmptyState() {
        this.grid.innerHTML = `
          <div class="ai-blog-empty-{{ ai_gen_id }}" style="grid-column: 1 / -1;">
            <h3>No posts found</h3>
            <p>There are no posts in this category yet.</p>
          </div>
        `;
      }

      updateLoadMoreButton() {
        if (this.hasMorePosts && this.grid.children.length > 0) {
          this.loadMoreContainer.style.display = 'block';
        } else {
          this.loadMoreContainer.style.display = 'none';
        }
      }

      restoreScrollPosition() {
        if (this.scrollPosition > 0) {
          setTimeout(() => {
            window.scrollTo(0, this.scrollPosition);
            this.scrollPosition = 0;
          }, 100);
        }
      }
    }

    customElements.define('blog-filter-{{ ai_gen_id }}', BlogFilter{{ ai_gen_id }});
  })();
</script>

{% schema %}
{
  "name": "Blog with AJAX filtering",
  "tag": null,
  "settings": [
    {
      "type": "header",
      "content": "Blog settings"
    },
    {
      "type": "text",
      "id": "page_title",
      "label": "Page title",
      "default": "Blog"
    },
    {
      "type": "blog",
      "id": "blog",
      "label": "Blog"
    },
    {
      "type": "range",
      "id": "posts_per_page",
      "min": 6,
      "max": 24,
      "step": 3,
      "label": "Posts per page",
      "default": 12
    },
    {
      "type": "header",
      "content": "Navigation"
    },
    {
      "type": "text",
      "id": "nav_all_label",
      "label": "All posts label",
      "default": "All"
    },
    {
      "type": "text",
      "id": "nav_1_label",
      "label": "Category 1 label",
      "default": "Latest News"
    },
    {
      "type": "text",
      "id": "nav_1_tag",
      "label": "Category 1 tag",
      "info": "Enter the exact tag handle"
    },
    {
      "type": "text",
      "id": "nav_2_label",
      "label": "Category 2 label",
      "default": "Guides & How To"
    },
    {
      "type": "text",
      "id": "nav_2_tag",
      "label": "Category 2 tag",
      "info": "Enter the exact tag handle"
    },
    {
      "type": "text",
      "id": "nav_3_label",
      "label": "Category 3 label",
      "default": "Athletes & Ambassadors"
    },
    {
      "type": "text",
      "id": "nav_3_tag",
      "label": "Category 3 tag",
      "info": "Enter the exact tag handle"
    },
    {
      "type": "text",
      "id": "nav_4_label",
      "label": "Category 4 label",
      "default": "Community"
    },
    {
      "type": "text",
      "id": "nav_4_tag",
      "label": "Category 4 tag",
      "info": "Enter the exact tag handle"
    },
    {
      "type": "text",
      "id": "nav_5_label",
      "label": "Category 5 label"
    },
    {
      "type": "text",
      "id": "nav_5_tag",
      "label": "Category 5 tag",
      "info": "Enter the exact tag handle"
    }
  ],
  "presets": [
    {
      "name": "Blog with AJAX filtering"
    }
  ]
}
{% endschema %}